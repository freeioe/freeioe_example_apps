local class = require 'middleclass'

local index = class('tag_db.index')

function index:initialize(folder)
end

--- Returns Database Objects if exists
function index:open(key, stime)
end

function index:search(key, stime, etime)
end

function index:
